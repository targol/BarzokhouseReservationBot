# دستورالعمل‌های پایدار پروژه ربات خانه برزک (Barzok House Telegram Bot)

## قانون ماندگاری اطلاعات کاربر (Data Preservation & Protection)
1. **عدم بازنویسی اتاق‌ها و غذاها (Never Overwrite Rooms & Foods):**
   - به هیچ عنوان اطلاعات اتاق‌ها (نام‌ها، توضیحات، نرخ‌های هر نفرشب، عکس‌ها) و اطلاعات غذاها (نام‌ها، قیمت‌ها، توضیحات، وعده‌های مجاز) را به داده‌های اولیه یا پیش‌فرض بازنگردانید.
   - تغییراتی که کاربر در اتاق‌ها و غذاها اعمال کرده باید همیشه حفظ شود.
2. **جداسازی داده از کد (Separation of Data from Code):**
   - اطلاعات داده‌ای در فایل‌های `repo_files/rooms.json` و `repo_files/foods.json` نگهداری می‌شوند و `repo_files/config.py` این فایل‌ها را اولویت‌بندی می‌کند.
   - در تمام ویرایش‌های آینده، فایل‌های JSON داده‌های کاربر را به عنوان منبع حقیقت (Source of Truth) حفظ کنید و آنها را ریست نکنید.
3. **ذخیره‌سازی در اپلیکیشن وب:**
   - هر تغییری که در پنل وب توسط کاربر انجام می‌شود باید در `localStorage` پایدار بماند تا با رفرش یا بارگذاری مجدد برنامه اطلاعات کاربر از دست نرود.
